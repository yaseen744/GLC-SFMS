import StudentsPage from "./StudentsPage.jsx";

// "All Students" — shows every student across every class/section.
export default function Students() {
  return <StudentsPage lockedClass={null} />;
}
